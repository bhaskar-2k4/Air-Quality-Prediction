# Air-Quality-Prediction
A Flask-based Air Quality Prediction web application that uses a KNN machine learning model to predict air quality from environmental data. The project includes data preprocessing, model training, and an interactive web interface for predictions.
